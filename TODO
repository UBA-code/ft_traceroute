- if second prob not timed out, must check what's happen
- remove the udp packet init function if not mandatory
- try to switch to SO_RCVTIMEO with setsockopt instead of select for better performance
